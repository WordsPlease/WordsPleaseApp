import React from 'react';
import { View } from 'react-native';

const DetailSizeTest = (props) => {
  <View style={{flex: 1}}>
    <View style={{flex: 4, backgroundColor: '#1D8348', borderWidth: 1, borderColor: '#000'}}
  </View>
}

export default DetailSizeTest;
